<template repeat="*"  name="echo of input deck" id="echoinputx" 
   pattern="\s*\=+\s+echo of input deck \=+\s*" endPattern="\s*\=+\s*" endOffset="1">
    <comment>
============================== echo of input deck ==============================
echo

start dft_feco5

# test of DFT with Fe(CO)5 using cd basis and xc basis

# There are 2 million integrals

...
dft
  xc b3lyp
end
title "case t23 --- (b3lyp) DFT (energy)"
task dft energy



================================================================================
    </comment>
    <!-- 
  <templateList>
    <template repeat="*" id="geom" pattern="\s*geometry\s+.*" endPattern="\s*end\s*" endOffset="1">
      <record repeat="*" id="geomx">{X,n:geo}</record>
    </template>
    <template repeat="*" id="dft" pattern="\s*dft\s*" endPattern="\s*end\s*" endOffset="1">
      <record repeat="*" id="dftx">{X,n:dft}</record>
    </template>
  </templateList>
 -->
   <transform process="debug" xpath="."/>
 </template>
