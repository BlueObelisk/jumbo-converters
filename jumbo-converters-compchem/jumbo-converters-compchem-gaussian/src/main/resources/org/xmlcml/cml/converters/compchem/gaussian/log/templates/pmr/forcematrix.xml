<template id="forcematrix" name="Full mass-weighted force constant matrix" repeat="*"
    pattern="\s*Full mass-weighted force constant matrix:.*" 
    endPattern="\s*" endOffset="1"
    xmlns:xi="http://www.w3.org/2001/XInclude"
    >
    
  <comment class="example.input" id="forcematrix">
 Full mass-weighted force constant matrix:
  </comment>

  <record id="forcematrix" repeat="*">{X,x:forcematrix}</record>

  <comment class="example.output.raw" id="forcematrix">
 </comment>
</template>
 